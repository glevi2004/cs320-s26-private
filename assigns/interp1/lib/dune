(library
 (name interp1)
 (libraries fmt menhirLib))

(ocamllex lexer)

(menhir
 (modules parser)
 (flags --table))

(rule
 (action (with-stdout-to syntaxError.ml
  (run menhir
   %{dep:parser.mly}
   --compile-errors %{dep:syntaxError.messages}))))
